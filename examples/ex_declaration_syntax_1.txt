mod main
  // declarations (const, type, var, fun)
  const
    i = 5;
  type
    celsius = integer;
    // optional morph block (mu/um)
    mu
      // some code for morphing a celsius type into a farenheit type
    um
    farenheit = integer;
    mu
      // code for morphing into a celsius type
    um
    employee = rec
      id:str;
      name:str;
    cer
    mu
      // code for morphing employee type into student type
    um 
    student: rec
      id:str;
      name:str;
    cer 
    mu
      // code for morping student type into employee type
    um
    int = integer;
  var
    a:int;
    b:str;
    m:employee = {id = "W1", name = "Meehan"}
  fun
    // Procedure syntax
    foo()
      //optional declarations for foo() here
      begin
        // foo code here
      end foo;
    // Function syntax
    bar(x:integer, y:integer): real
      // optional declarations for bar() here
      begin
        // bar() code here
      end bar;
dom main
